apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - www.local.dicemagic.io
      - api.local.dicemagic.io
      secretName: letsencrypt-certs
  rules:
  - host: www.local.dicemagic.io
    http:
      paths:
      - path: /
        backend:
          serviceName: www-service
          servicePort: 80
  - host: api.local.dicemagic.io
    http:
      paths:
      - path: /
        backend:
          serviceName: dicemagic-service
          servicePort: 80
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: grpc-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
  namespace: default
spec:
  tls:
    - hosts:
      - grpc.local.dicemagic.io
      secretName: letsencrypt-certs
  rules:
  - host: grpc.local.dicemagic.io
    http:
      paths:
      - backend:
          serviceName: dicemagic-service
          servicePort: grpc