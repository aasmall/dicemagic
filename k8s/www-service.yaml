apiVersion: "v1"
kind: "Service"
metadata:
  name: "www-service"
  namespace: "default"
  labels:
    app: "www"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "www"
  type: "NodePort"
